# DB Summary: test1

This is a placeholder summary file for “My DB”.

If you generated an actual schema summary via DB Manager → My DB, you can replace this content with the real output and re-upload to the app’s vector store from the UI.

- Created: 2025-09-26
- Filename: mydb-summary-test1-20250926-104405.md

Sections to include when you update this file:
- Tables overview (name, purpose, row estimate)
- Columns per table (type, nullable, defaults, comments)
- Foreign keys (relationships) per table
- Optional: Key views and their simplified definition

Tip: Keep summaries scoped per domain/module for best retrieval quality.

## Tables

| Table | Purpose | Approx. Rows | Primary Key | Foreign Keys | Notes |
|---|---|---:|---|---|---|
| example_table | Short description of what this table stores | 0 | id | user_id → users.id | Replace with your real info |

Add one row per table. Keep descriptions short and practical.

## Columns (per table)

- example_table
  - id INT NOT NULL PRIMARY KEY
  - user_id INT NOT NULL -- references users.id
  - name VARCHAR(255) NOT NULL
  - created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP

Repeat for each table that agents or prompts will query.

## Foreign Keys

- example_table.user_id → users.id

List only the relationships that matter for common queries.
