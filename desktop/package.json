{
  "name": "mcp-desktop-client",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Electron desktop client for MCP WebSocket",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "build": "electron-builder -c.extraMetadata.main=main.js"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-builder": "^24.13.3"
  }
}

