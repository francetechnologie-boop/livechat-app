# DB Manager PostgreSQL

Module providing PostgreSQL profile management.

- Backend routes under `/api/db-postgresql/*` (self-mounted JSON parser)
- Table: `mod_db_postgresql_profiles`
- Frontend page exported as `Main` from `frontend/index.js`
- MCP flags: hasMcpTool + hasProfil (see `module.config.json`)

